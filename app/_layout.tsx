// Expo Router layout is no longer used now that navigation is handled
// via App.tsx + React Navigation. This file is kept as a stub so that
// any leftover router entry points do not crash.
import React from "react";

export default function RootLayout() {
  return null;
}

